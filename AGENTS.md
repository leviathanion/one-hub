# 项目协作原则

## 代理职责与协议边界

本项目的核心职责是协议透传和必要的协议转换，不是上游 API 的前置参数校验器。

- 在 exact-wire 或无需解释相应字段的数据路径中，应保持客户端 wire 语义并透传未知字段、联合类型和未来扩展字段；不得为复制上游校验而引入封闭字段白名单。
- 代理只校验自身负责的边界：本地协议 envelope、已承诺的支持面、渠道能力与可表示性、传输和有界资源约束、安全、权限、计费及资源生命周期。上游拥有的参数语义交给真实服务端校验。
- 跨协议适配器必须明确其转换边界。无法无损表示时，应在 provider work 前返回明确错误，或由选路层选择能力匹配的渠道；适配器不得自行隐式换渠道、丢字段、构造伪造的空值或触发 panic。
- 一旦上游可能已经观察请求或产生费用、状态或副作用，不得因转换或传输失败隐式换渠道或重试，除非该操作已有明确的重试契约。
- exact-wire 路径应保留上游状态和错误协议；跨协议路径只做明确的协议映射。代理必须移除自身拥有的凭据、账户信息及其他不可向客户端暴露的数据。

## 复杂度控制

- 每项业务规则只能有一个语义所有者。认证、准入、选路、资源生命周期和结算由 relay 或共享 policy 负责；provider/adapter 只负责上游协议构造、转换和证据提取；transport 只负责 I/O 与交付事实。不得在多个层级分别实现同一判断。
- provider 差异应收敛在 capability、provider 或 adapter 边界。不得在通用 relay、controller 或 transport 中持续增加按 provider 类型分支；确属跨 provider 的共同规则才进入共享层。
- 协议处理必须明确属于 exact-wire、same-dialect 或 cross-protocol：exact-wire 保留原始 wire 语义，不为观察、日志或计费而无意义地反序列化并重新序列化；same-dialect 只修改代理明确拥有的字段；cross-protocol 只实现有明确语义的映射，不能表示时显式失败。
- 渠道能力、资源 owner 和请求可表示性必须在 provider work 前确定。adapter 不负责选路，也不得通过运行时试错、静默降级或清空字段使请求“能够发送”。
- 重试不是通用默认能力。一旦上游可能已经观察请求或产生费用、状态或副作用，默认不得重试或切换渠道。新增重试前必须明确可重放条件、幂等边界、响应提交点、计费影响和失败后的最终状态。
- 新增跨请求、并发或分布式状态前，必须说明其唯一事实来源、key 作用域、生命周期、容量上限、失败语义和清理方式。能够从现有事实安全派生的状态不持久化；缓存和 Redis 不得在没有明确一致性协议时升级为权威 owner。
- 不为假想兼容性增加 feature flag、legacy 分支、双实现或额外配置层。确需迁移期并存时，必须记录外部契约、默认路径、结束条件和旧路径删除条件；不存在真实迁移要求时，在同一变更中删除被替代路径。
- 协议边界变更至少验证：未知字段透传、不可表示输入在 provider work 前失败、上游错误语义保留，以及歧义执行后不会发生未授权重试。
